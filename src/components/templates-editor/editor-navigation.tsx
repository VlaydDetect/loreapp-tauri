import React from 'react';

type Props = {
    
}

const EditorNavigation: React.FC<Props> = ({  }) => {
    return (
        <div>
            
            </div>
    );
};

export default EditorNavigation;
